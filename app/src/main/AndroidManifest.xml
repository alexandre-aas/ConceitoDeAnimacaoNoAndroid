<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.s.d.a.a.conceitodeanimacaonoandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConceitoDeAnimacao_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".animacoes.AlphaAnimacao" />
        <activity android:name=".animacoes.RotateAnimacao" />
        <activity android:name=".animacoes.TranslateAnimacao" />
        <activity android:name=".animacoes.ListaDeAnimacoes" />
    </application>

</manifest>